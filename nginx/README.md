# Nginx Configuration

Purpose: Hosts the frontend SPA and proxies API requests to PHP-FPM. Include configuration for SPA fallback, static asset caching, and blocking direct access to uploads.

Quick TODO

- [ ] Provide `default.conf` tuned for Slim 4 routing
- [ ] Add security headers and strict transport rules for production
